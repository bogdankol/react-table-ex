import { EColumns } from '../types'

export const COLUMNS: EColumns[] = [
	EColumns.name,
	EColumns.country,
	EColumns.web_pages,
	EColumns.state_province,
]

export const countries = [
  'china', 'united states', 'turkey', 'united kingdom', 'canada', 'germany'
]
